/* useEffect( () => {

    document.getElementById("search-bar").addEventListener("submit", handleSearch)
    
    return () => { document.getElementById("search-bar").removeEventListener("submit", handleSearch) 
  
  }
  },[parcels]
  ) */